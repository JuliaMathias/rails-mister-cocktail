<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.association :ingredient, input_html: { class: "select2 beautiful-dropdown" }, wrapper_html: { class: 'd-flex flex-column' }, include_hidden: false %>
  <%= f.input :description %>
  <%= f.button :submit, class: "btn btn-secondary" %>
<% end %>
