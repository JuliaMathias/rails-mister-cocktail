<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.association :ingredient, input_html: { class: "beautiful-dropdown select2" }, include_hidden: false %>
  <%= f.input :description %>
  <%= f.button :submit, class: "btn btn-secondary" %>
<% end %>
